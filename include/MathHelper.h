#ifndef MATH_HELPER_INCLUDE
#define MATH_HELPER_INCLUDE

extern unsigned clamp(unsigned upper, unsigned lower, unsigned value);

extern float safeDivison(float dividend, float divisor);

#endif // MATH_HELPER_INCLUDE
